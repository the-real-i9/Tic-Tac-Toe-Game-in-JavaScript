import { player1, opponent, currentPlayer } from './gameStartInit.js';
const onePlayerGamePlay = () => {
    console.log({
        player1,
        opponent,
        currentPlayer,
    });
};

export default onePlayerGamePlay;
